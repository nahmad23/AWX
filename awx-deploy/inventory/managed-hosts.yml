---
# The 10 servers that AWX will MANAGE (the targets of the ping job).
# This list is also pushed into AWX as inventory hosts by configure-awx.yml.
#
# NOTE: "server3" was corrected to include the missing dot
#       (was "server3unitedlex.global" in the original request).
all:
  children:
    unitedlex_servers:
      hosts:
        server1.unitedlex.global:
        server2.unitedlex.global:
        server3.unitedlex.global:
        server4.unitedlex.global:
        server5.unitedlex.global:
        server6.unitedlex.global:
        server7.unitedlex.global:
        server8.unitedlex.global:
        server9.unitedlex.global:
        server10.unitedlex.global:
